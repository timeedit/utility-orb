description: >
    Creates /tmp/tesafetoken from TE_SAFE_TOKEN env.
    When executing command uses TE_SAFE_CONTEXT as env and secret name as argument
steps:
  - run:
      name: Auth GCP
      command: <<include(scripts/generate_get_secret)>>
